services:
  mysql:
    image: 'mysql:8.4'
    environment:
      MYSQL_DATABASE: mydb
      MYSQL_PASSWORD: ek
      MYSQL_ROOT_PASSWORD: secret
      MYSQL_USER: ek
    ports:
      - '127.0.0.1:3306:3306'